using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Web.Http;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using ImagesDownloader;
using ImagesDownloader.Controllers;

namespace ImagesDownloader.Tests.Controllers
{
    /*[TestClass]
    public class ValuesControllerTest
    {
        [TestMethod]
        public void Get()
        {
            // Arrange
            JobsController controller = new JobsController();

            // Act
            IEnumerable<string> result = controller.GetById(1);

            // Assert
            Assert.IsNotNull(result);
            Assert.AreEqual(2, result.Count());
            Assert.AreEqual("value1", result.ElementAt(0));
            Assert.AreEqual("value2", result.ElementAt(1));
        }

        [TestMethod]
        public void GetById()
        {
            // Arrange
            JobsController controller = new JobsController();

            // Act
            string result = controller.GetById(5);

            // Assert
            Assert.AreEqual("value", result);
        }

        [TestMethod]
        public void Post()
        {
            // Arrange
            JobsController controller = new JobsController();

            // Act
            controller.Post("value");

            // Assert
        }
    }*/
}
